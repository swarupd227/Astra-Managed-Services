import { ArtifactPage } from '@/workspace/cards/view'

/** The page rendering of the agent lifecycle view. The figures live with the card. */
export function AgentLifecycle() {
  return <ArtifactPage kind="agentLifecycle" />
}
