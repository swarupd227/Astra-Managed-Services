import { ArtifactPage } from '@/workspace/cards/view'

/** The page rendering of the engagement and acceleration view. The figures live with the card. */
export function Acceleration() {
  return <ArtifactPage kind="acceleration" />
}
