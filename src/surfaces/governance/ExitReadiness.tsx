import { ArtifactPage } from '@/workspace/cards/view'

/** The page rendering of the renew-or-exit view. The figures live with the card. */
export function ExitReadiness() {
  return <ArtifactPage kind="exit" />
}
