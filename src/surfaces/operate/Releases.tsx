import { ArtifactPage } from '@/workspace/cards/view'

/** The page rendering of the releases view. The figures live with the card. */
export function Releases() {
  return <ArtifactPage kind="releases" />
}
