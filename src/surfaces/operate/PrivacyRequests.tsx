import { ArtifactPage } from '@/workspace/cards/view'

/** The page rendering of the privacy view. The figures live with the card. */
export function PrivacyRequests() {
  return <ArtifactPage kind="privacyRequests" />
}
